# GAPS.md

A ledger of repo / agent-setup capabilities **deliberately not built yet**, each with the trigger that should wake it. A fresh template instance is typically pre-code, solo, and client-side, so most tooling a mature repo carries would have nothing to act on yet — no dependency graph to scan, no UI to guard, no deadline to age against. This file records *why* each deferred thing is absent and *what* should bring it back, so a deferred item resurfaces when its trigger fires instead of being silently forgotten or re-litigated.

Scope: the repo's tooling, CI, agents, skills, and process scaffolding. Not a product backlog — features/tools live in issues and [`DESIGN.md`](DESIGN.md), not here.

This file is itself drift-prone, so it sits in the [`AGENTS.md`](AGENTS.md) "Keeping docs and drift-prone files current" Update Triggers logic: when a process or roadmap change fires or retires one of the triggers below, reconcile this file in the same PR. A row whose trigger has already fired but is still parked under "Deferred" is a finding — raise it the way that section says (a non-blocking IMPORTANT note, never a merge blocker). Don't restate `AGENTS.md`, `DESIGN.md`, or `SECURITY.md` here; cross-reference them.

How to use a row: each is `Item | Trigger that should wake it | Why deferred`. Add a row when you consciously skip a capability that a future state will need; strike it through and annotate **WOKEN** (with a date) when its trigger fires and you build the thing in the same PR; delete it only once the woken note has outlived its usefulness.

---

## Deferred (build when the trigger fires)

The ledger starts **empty for a fresh instance** — there is no inherited backlog to carry. Populate it as you make deliberate deferral decisions for *this* product (e.g. dependency-hygiene tooling once a `package.json` and lockfile land, a commit-gated lint hook once there is shipping code, additional repo agents/skills once they exist).

Deferrals from the deployment/orchestration architecture ([`docs/decisions/0001`](docs/decisions/0001-deployment-orchestration-and-swappability.md)):

| Item | Trigger that should wake it | Why deferred |
| --- | --- | --- |
| Next-free `core` workspace package (extract domain/llm/pipeline/engine/store) | ~~The first cloud-deploy PR — the one adding the pipeline Job's Dockerfile~~ — **FIRED 2026-06-19, consciously RE-DEFERRED**; re-wake on image-pull-latency pain or a published-package need | The Job ships single-repo. Boot LATENCY (distinct from idle cost / image size) was addressed in #162: the per-generation `run-job` ENTRYPOINT now boots from a COMPILED esbuild bundle (`node dist/job/run-job.js`, via `npm run build:job`) instead of transpiling TS with `tsx` per cold start. The remaining split (a smaller image dropping next/tsx from the Job runtime) is a SIZE optimization, not a blocker — a scale-to-zero Job idling at $0 doesn't care about ~80MB of unused layers, the full `npm ci` is KEPT (the migrate Job still command-overrides this image to run `tsx src/store/migrate.ts`), and the import fence already guarantees the core is Next-free. Coupling is packaging-only + extraction stays a cheap file-move. |
| ~~Terraform / GCP deploy foundation (Cloud SQL, Secret Manager, Artifact Registry, IAM, GCS state)~~ — **WOKEN 2026-06-19** | ~~The local e2e produces a curriculum end-to-end~~ — fired | Applied: `infra/` Terraform provisioned `topic-synthesis-prod` — Cloud SQL Postgres 16 + registry + secrets + runtime SA. The remaining **Cloud Run Service+Job + WIF** are the active deploy PRs (the `GcpEngine` + `core`-workspace rows below cover the code half). |
| ~~`GcpEngine` (Postgres `step_results` adapter behind the `Engine` seam)~~ — **WOKEN 2026-06-19** | ~~Cloud-deploy phase~~ — built ahead of the Cloud Run Job | Built: `src/engine/gcp-engine.ts` + the `step_result` table — durable per-run step memoization (crash-resume reads completed steps back, never re-running paid work) + in-process fan-out dedup. Wired into the Cloud Run Job in the deploy PRs. |
| DBOS Transact (durable-execution library upgrade behind the same seam) | Durable-execution fragility: queue fan-out beyond `Promise.all`, human-in-the-loop waits, or crash-resume corner cases | The DIY `step_results` table suffices at current scale (~30–50 steps/run, bursty, solo). |
| Cloud Tasks (fan-out queue) | Synthesis fan-out must outlive one Job execution, OR cross-run rate-limiting against provider RPM/token caps | In-process `Promise.all` over one Job covers a ~10–15-node run. |
| Cloud Workflows / multi-task Job sharding | Cross-job orchestration, HIL pauses, or fan-out beyond one instance's RAM/CPU | Code-orchestration in one Job is the right fit; managed orchestrators add payload-limit + decomposition cost (see ADR §2). |
| Remove vestigial Redis from `docker-compose.yml` | A docker-compose / local-infra cleanup PR | Redis backed the dropped Trigger.dev; harmless but unused now. |
| ~~`@eleatic/eval` trace seam (`src/trace`)~~ — **WOKEN 2026-06-19** | ~~`@eleatic/eval` is published to npm (currently 404)~~ — fired: published `@0.1.0` | Built in this PR: the `TraceSink` observability port + `src/trace` (span/reduce/eleatic-adapter) + the `--trace` CLI flag, fenced so the package stays out of the app bundle. Was blocked on the sibling's npm publish. |
| `runPipeline` options-object refactor (collapse the 6 positional params) | `runPipeline` gains a 7th positional param, OR a caller mis-binds the positional args | The `TraceSink` (6th, after `stages`) was added positionally + defaulted so it's purely additive — zero blast radius on the 9 existing positional-arg tests. An options object is the cleaner shape once the arity grows again. |
| `src/app` → pipeline import boundary (the Service triggers the Job, doesn't run the pipeline) | The deployed Cloud Run Job model replaces in-process generation | The lean e2e runs the pipeline IN-PROCESS inside `src/app` (the Service) + reads the store from server components/route handlers, so a blanket `src/app`→pipeline/store ban would be wrong today. The fence ships with `core-no-frontend` only (ADR 0001 §4). |
| Full CI app-gates (run typecheck/test/build in CI) | The `core` workspace split lands, or flakiness slips past the bot review | PARTIAL: `.github/workflows/import-fence.yml` now does `npm ci` + `npm run lint:boundaries` in CI (the first code-level gate + the npm-ci-in-CI pattern). Extending it to typecheck/test/build is the remaining step; the `@julianken-bot` per-PR review (throwaway-clone gates) still covers those today. |
| Promote `frontend-e2e.yml` (the e2e + visual suite) to a REQUIRED check | The suite has run green across a handful of PRs AND the Figma-rebuild visual baselines have settled (the dated-flip) | PARTIAL — the ruleset half has fired: `e2e + visual (soft)` IS now in the live `main` branch ruleset's required status checks (alongside `fence`/`validate`), so it **does gate merges** — a red e2e/visual blocks the Mergify queue (surfaced as a ruleset condition; the `.mergify.yml` `queue_conditions` e2e lines stay commented, so the requirement is ruleset-side). The workflow job, however, still ships `continue-on-error` and is still NAMED `e2e + visual (soft)`, so the WORKFLOW-level "soft-launch" label is now stale relative to the ruleset (a non-green render must still be fixed/re-baselined in-PR, not waved through). Remaining to FULLY promote: drop `continue-on-error` + rename off `(soft)` — an orchestrator settings/workflow change in a separate PR. |

Deferral from the single-lesson refocus ([`docs/decisions/0003`](docs/decisions/0003-single-lesson-refocus-and-retained-curriculum-machinery.md)):

| Item | Trigger that should wake it | Why deferred |
| --- | --- | --- |
| `curriculum`/`curriculum_page` **table-name** rename (leading topology: `/lesson` = the atom, a future `/curriculum` = the wrapper hub) | The curriculum-wrapper milestone lands (decompose → N lessons → tiered hub), un-dormant-ing `runPipeline` — at which point the atom-vs-wrapper topology becomes real | The `/lesson` ROUTE + read-path identifiers (`getLesson`, `lessonId`, …) were renamed off the dead noun (#172, executing ADR-0003 §4); only the **table names** still reuse the v1 `curriculum` row as code identifiers (ADR-0003 §3), and a bare table rename buys nothing until the wrapper exists (and would need bespoke guarded DDL — `migrate.ts` re-applies `schema.sql` wholesale). **No `page.href` redirect shim was ever needed:** hrefs are rebuilt at READ time from `concept_slug` (`rebuildHub`), never persisted as a column, so the route rename 404s nothing. The retained machinery is tagged `DORMANT:`/`RETAINED:` and fenced out of `scripts/check-concept-drift.sh`. |

Deferral from the workflow-telemetry seam ([#166](https://github.com/julianken/topic-synthesis/issues/166)):

| Item | Trigger that should wake it | Why deferred |
| --- | --- | --- |
| Local-dev / `InlineEngine` `EventSink` parity — the `api/generate` in-process fallback + `run-skeleton` emitting the same `WorkflowEvent`s the deployed Job does | A need to diagnose per-phase timing/cost on a LOCAL run, OR `InlineEngine` gains a telemetry consumer | The #166 prod path emits via `GcpEngine`; `InlineEngine` carries no event plumbing, so parity means threading an `EventSink` through it + both callers. It is console-only (never reaches Cloud Logging) and explicitly NOT required for the #167 dashboard, so it was scoped out of PR #169. |

Deferral from the soft-delete data layer ([#198](https://github.com/julianken/topic-synthesis/issues/198)):

| Item | Trigger that should wake it | Why deferred |
| --- | --- | --- |
| Partial index `CREATE INDEX … ON curriculum (owner_sub) WHERE deleted_at IS NULL` to keep the (now deleted-filtered) `listLessons` / `getLesson` owner-scoped reads fast | The `curriculum` row count grows enough that the library-home / lesson reads scan a meaningful number of soft-deleted rows (a measurable read-latency regression, or many lessons accumulating in Recently-deleted before purge) | #198 added the `deleted_at IS NULL` predicate to the three owner-scoped reads but **no index**: at solo pre-scale volume a seq/owner-index scan is free, and an unused partial index is pure write/maintenance cost. The migrate runner re-applies `schema.sql` wholesale, so adding it later is a one-line additive `CREATE INDEX IF NOT EXISTS`. (Auto-purge of long-deleted rows is a separate concern tracked under the deletion epic, not here.) |

Deferral from the preserved completed-workflow page ([#232](https://github.com/julianken/topic-synthesis/issues/232) — run-lifecycle 3/4):

| Item | Trigger that should wake it | Why deferred |
| --- | --- | --- |
| A retention / partial-index sweep for the now-durable `research_event` rows (e.g. `CREATE INDEX … ON research_event (run_id)` already exists, but no auto-purge of old runs' feeds, and the `getResearchEvents` read is not index-tuned for scale) | The `research_event` row count grows enough that the per-run read (the frozen `/workflow` band) or table size becomes a measurable cost — i.e. many lessons accumulate retained feeds (mirrors the #198 partial-index deferral) | #232 stopped pruning `research_event` at persist so the frozen `/lesson/[id]/workflow` page can replay the RESEARCH band, which makes the table grow unbounded with the lesson corpus (like `step_event` since #175). At solo pre-scale volume that growth is free and the existing `idx_research_event_run` covers the per-run read, so a retention/auto-purge policy + any read-tuning index is premature write/maintenance cost. The migrate runner re-applies `schema.sql` wholesale, so adding an index later is a one-line additive `CREATE INDEX IF NOT EXISTS`; auto-purge of long-finished runs is the broader concern this row tracks. |

Deferrals from the v11 lesson-workspace epic (parent [#79](https://github.com/julianken/topic-synthesis/issues/79) / [`docs/plans/lesson-workspace.md`](docs/plans/lesson-workspace.md)). The ADR-0003 route/topology shim above **stays parked** under this epic — decision 9 keeps that machinery dormant (no `runPipeline`/`graph.ts` revival), so this block **cross-references** that row rather than duplicating it. **NOTE (v11 pipeline revert):** the Phase-2 SYNTHESIS rework (typed sectioned `LessonSpec`, `specV11`, the graded critic, the `code`-stage workspace prompt + `postMessage` SENDER) was REVERTED to the pre-v11 blob-binary pipeline (see the SUPERSEDED banner in `docs/plans/lesson-workspace.md`). The KEPT reader-shell chrome / morph / generating-rail / serve-time §0 injection survive; the rows below that presuppose the v11 pipeline (an in-iframe progress SENDER, a graded-critic gate, a typed spec) are kept only as direction for a future re-land, not as descriptions of shipped behavior.

| Item | Trigger that should wake it | Why deferred |
| --- | --- | --- |
| Pixel-exact stable-spine render-harness verification — a headless renderer that checks the stable-spine / no-jitter / no-overflow invariants by computed geometry (the repo carries no jsdom/puppeteer/playwright today) | The critic's source-static proxies prove insufficient — i.e. a lesson passes the critic but the owner `HIL:` visual gate (TS-21/TS-25) rejects it for a spine/jitter/overflow defect the static checks can't see | No headless renderer in-tree, and an LLM can't compute `getBoundingClientRect` from source; the owner visual gate (decision 10) covers the residual fidelity gap for now. Open-question 3. |
| In-iframe `postMessage` progress SENDER — the synthesis `code` stage emitting the coordinate-only `lesson:progress` sections+scroll contract the reader shell's `validateMessage` receiver is already built for | A future UI-only (or pipeline) PR re-adds a progress sender; the reader-shell RECEIVER (`lesson-message.ts` / `reader-shell.tsx`) is KEPT and waits at its zero state until then | The v11 `code` prompt that emitted this SENDER was reverted with the blob-binary pipeline; the validated receiver chrome stays, so re-adding a sender (in-iframe or chrome-only) lights up the progress bar / section-jump with no boundary change. Decision 12. |
| In-iframe TOC / dot-scrubber parent-coordination — the in-iframe table-of-contents / dot-scrubber coordinated with the parent reader shell | The progress SENDER row above lands (the scrubber coordinates off the same `lesson:progress` payload the receiver validates) | The reader shell + receiver are built (TS-20, KEPT); the in-iframe coordination needs a sender to coordinate against, which was reverted with the v11 `code` prompt. Pairs with the progress-SENDER row above. |
| Typed gloss/mini-figure apparatus as first-class `LessonSpec` fields + a schema `≤3`/`≤1` cap | A milestone re-introduces a typed sectioned spec (the v11 synthesis rework was reverted to blob-binary — see `docs/plans/lesson-workspace.md`) and promotes glosses to typed apparatus | The whole typed-sectioned-spec / graded-critic Phase-2 synthesis rework was REVERTED to the blob-binary pipeline; a typed gloss cap presupposes that contract and would re-land with it, not before. |

Deferral from the repo-description consistency mechanism:

| Item | Trigger that should wake it | Why deferred |
| --- | --- | --- |
| Nightly/scheduled GitHub-description drift gate (compare the live `gh` description to the `INSTANCE.md` canonical; open a `drift:docs` issue on mismatch) | The reviewer `--check` + orchestrator post-merge sync stops sufficing — i.e. the live description drifts **out-of-band** (edited directly on GitHub) more than once | Introduction-time drift is already covered: the reviewer runs `scripts/sync-repo-description.sh --check` on identity-touching PRs and the orchestrator syncs post-merge. A scheduled gate would catch only out-of-band edits, and the repo has **no nightly CI** to extend yet — not worth standing one up for a single string until out-of-band drift actually recurs. |

Deferral from the lesson-deletion epic ([#207](https://github.com/julianken/topic-synthesis/issues/207) — the dedicated docs-reconcile slice of the recoverable soft-delete epic; sibling issues #198–#206):

| Item | Trigger that should wake it | Why deferred |
| --- | --- | --- |
| Time-based **auto-purge** of soft-deleted lessons — a retention policy / scheduled job that ages out `deleted_at`-stamped lessons after a fixed window | Retention/storage pressure to reclaim space from long-deleted lessons, OR a product decision to age out lessons left in the Recently-deleted shelf past a fixed window | **Manual permanent-delete ships in v1** — the Recently-deleted shelf's "Delete permanently" affordance ([#205](https://github.com/julianken/topic-synthesis/issues/205)) lets an owner purge a soft-deleted lesson on demand, so **only the *time-based automatic* purge is parked**: there is no retention window or scheduled purge job yet, and on-demand permanent-delete covers reclamation until deleted lessons accumulate enough to need automatic aging. |

Deferrals from the degrade-reason gate channel ([#214](https://github.com/julianken/topic-synthesis/issues/214)):

| Item | Trigger that should wake it | Why deferred |
| --- | --- | --- |
| The general **"every quality gate emits a `DegradeReason`"** AGENTS.md convention (a uniform gate→reason contract, e.g. a `'coverage'` gate + the live `coverage_below_threshold` code, a pedagogy/a11y-critic code) | A **second** live degrade producer lands — the `coverage-gate` un-dormants with the curriculum-wrapper milestone (epic #52), OR a pedagogy/a11y critic ships — so there is more than one gate to standardize | Today there is exactly ONE live producer (the `critic`) plus one synthesis try/catch; the dormant `coverage-gate` and the not-yet-existing pedagogy/a11y critics emit nothing. Inventing a cross-gate convention for one producer is premature abstraction — `DegradeReason` carries a reserved `coverage_below_threshold` code (document-but-don't-emit) so the shape is ready, but the *convention* waits for a real second gate. |
| **Owner-visible critique surfacing** — showing the degrade reason on the degraded-lesson reader (the "See what happened" entry) instead of operator-only Cloud Logging | A product decision that an owner should see WHY their own lesson degraded (paired with the resolution that the surfaced text is owner-safe), OR Issue 3's critic-remediation work makes the reason learner-actionable | `degradeDetail` is the raw critic `critique` / error message — the first free-text model output on the log stream, kept OPERATOR-ONLY behind the leak-safe boundary (no `concept_page` column; the owner-readable stores carry no raw-model-text column on purpose). Surfacing it to an owner needs a deliberate safety pass (bounding, sanitization, an owner-safe rephrase) that is out of scope for a telemetry-only channel; the disclosure stays the operator's. |
| Offline **critic-calibration sweep** (Issue 4) — replay a corpus of degraded briefs/artifacts through the critic (and model/prompt arms) to measure the false-reject rate keyed on the reason distribution | This `degrade_reason` telemetry has accumulated enough real critic-reject samples to calibrate against (the metric does NOT backfill — it fills from the first post-deploy run), AND Issue 3's critic-remediation lands needing a before/after measure | The sweep is **data-gated on this channel existing**: a calibration pass needs the reject-rate + reason distribution as its target signal, which did not exist until #214. It is the offline judge-sweep machinery (`--judge-sweep` / `judgeBriefsBatched`, #188) pointed at the critic arm — built only once there is a reason distribution to calibrate and a remediation to measure. |

Deferral from the Figma design-SoT lockstep convention ([`AGENTS.md`](AGENTS.md) → "Design source of truth" + the Update-Triggers Figma row):

| Item | Trigger that should wake it | Why deferred |
| --- | --- | --- |
| Figma SoT catch-up to current `main` — reconcile the frames that drifted while the read-only policy was in force (the frontend PRs that never updated Figma) | **Opportunistic, per-surface:** a PR that next TOUCHES a still-stale frame owes that surface's catch-up under the Figma Update-Triggers row. A deliberate baseline pass MAY bring the four canonical Feature-Screens frames (Sign-in `5:2` · Library `6:2` · Generating `1:2` · Lesson workspace `3:2`) to current `main`. Per the #226 amendment, when a still-stale surface is next changed via an implementation ISSUE the catch-up frame is authored at the **issue stage** (the PR reconciles only on divergence); an issue-less touch does the catch-up in that PR | The lockstep rule is FORWARD-LOOKING — it does not retroactively brand in-flight PRs as violations, and a big-bang redraw of every frame is disproportionate for a single-tenant app. Pre-existing drift is acknowledged debt; the per-surface trigger repairs the backlog naturally as surfaces are next touched (the reviewer does not block a PR for drift it didn't introduce — R7 analogue). |
| Figma MCP write access on the **issue-authoring AND implementer/PR dispatch paths** — both the issue-authoring agent (which now does the primary, design-first frame authoring at the issue stage) and the implementing agent's dispatch must carry the Figma MCP write tools (`use_figma` / `generate_figma_design` / `create_new_file` / `upload_assets`) so the issue-stage authoring and the in-PR reconcile are possible | A frontend issue is authored, or a frontend PR implemented, by a dispatch that lacks Figma MCP write access — until the write tools are wired into the standard issue-authoring AND implementer/PR dispatch paths | When a dispatch lacks write access the issue (or PR) writes `Figma update deferred — no MCP write access` and opens a `drift:docs` follow-up (never silently skip); wiring the Figma MCP write tools into the standard issue-authoring AND implementer/PR dispatch paths retires the workaround so issue-stage authoring + the in-PR reconcile become the default. |

Deferral from the honest held/failed disposition ([#215](https://github.com/julianken/topic-synthesis/issues/215)):

| Item | Trigger that should wake it | Why deferred |
| --- | --- | --- |
| A leak-safe `passed BOOLEAN` (or `held`) column on the lesson page row, to make the held-vs-failed distinction a RECORDED verdict fact instead of the incidental html-presence signal | The DORMANT curriculum coverage-degrade path (`runPipeline` → `assembleHub`) activates — it also persists `status='soon'` WITH content, so html-presence would no longer cleanly separate a critic-HELD lesson from a coverage-degrade | #215 derives `built \| held \| failed` in the READ path from `(status, html-presence)` — faithful on the LIVE `runLesson` path (a critic-reject keeps its rendered html → `soon` + html; a synthesis exception has none → `soon` + null), with NO schema change. The only path that could muddy the html signal is the curriculum coverage-degrade, which is dormant (no entrypoint drives `runPipeline`). Adding a `passed` column now is premature schema for a dormant path; record the verdict as a real column when that path wakes. |
