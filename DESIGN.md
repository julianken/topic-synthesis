# DESIGN.md — Topic Synthesis

> **This file is the whole truth for design.** An agent that has never opened this app must be able to rebuild any surface to pixel fidelity from this document alone. Every value here is concrete and current. Where this file and the working build disagree, the build wins; reconcile this file to match it.
>
> **Authority:** shipped build > `DESIGN.md` > Figma (see AGENTS.md → "Design source of truth"). A Figma design file is now configured as a **read-only design-direction reference** (`INSTANCE.md` → "Design / Figma (read-only)"); `DESIGN.md` remains the design source of truth and wins on any conflict — a disagreeing Figma value is reconciled into §0 here, never built from.

**This is v0**, scoped to the walking-skeleton chrome (intake form, curriculum hub, page tile, progress, sandboxed artifact frame). It grows as surfaces land. Keep raw literals in §0 only; reference tokens by name everywhere else.

---

## 0. Token Manifest

> **Reconciliation note (TS-3, epic #79).** Every delta below between the prior hex/sparse §0 and the locked v11 reference mock (`.superpowers/topic-synthesis-lesson-workspace-v11.html` `:root`, lines 47–80) is resolved **ADOPTED** (the mock is the locked direction per program decision 8 / open-question-5) or **KEPT-illustrative** with a one-line rationale. The exact per-step OKLCH/geometry values are transcribed from that mock; TS-18 materializes them in `globals.css` and the shipped build wins on any later tuning.
>
> **Two-copies invariant (TS-19).** The §0 manifest is materialized in **two** places that must agree value-for-value: `src/app/globals.css` (the SoT `:root`) and `src/app/artifact/serve.ts` (`ARTIFACT_ROOT_TOKENS`, the serve-time-injected `:root` that re-themes already-generated lessons). A §0 retoken MUST edit **both** in the same PR — touching only `globals.css` and forgetting `serve.ts` would silently serve the OLD theme to every existing lesson. This is CI-guarded in `serve.test.ts` by a VALUE-drift check that asserts each injected literal equals the resolved `globals.css` value (and that every injected token name exists in `globals.css`). (The generated lesson no longer carries inline `var()` fallbacks — that third copy in `src/pipeline/code.ts` was dropped with the v11 pipeline revert; serve-time injection re-themes the artifact instead.) Two carve-outs the artifact copy DOESN'T mirror: (a) the **translucent/gradient surface** tokens (`--app-bg`, the `--surface-*` alphas, `--brand-*`) are **chrome-only** — the opaque-origin artifact has no app cascade to float over, so they live in `globals.css` alone; (b) the **font stacks** (`--sans`/`--mono`) lead/embed a `next/font` CSS var (`--font-inter`/`--font-jetbrains-mono`) the sandbox can't resolve, so the artifact copy drops it and the drift check normalizes that one divergence before comparing.

**Color primitives** — OKLCH ramps (ADOPTED — perceptual-uniformity + the v11 locked direction; replaces the prior `#hex` `--c-*` ramps. The `-c-` prefix is dropped so token names match the mock 1:1; ADOPTED steps `--ink-850`, `--fog-50`, `--fog-450`, `--fog-550` extend the ramps the mock uses).
- `--ink-950 oklch(0.165 0.018 250)` · `--ink-900 oklch(0.205 0.020 250)` · `--ink-850 oklch(0.245 0.020 250)` · `--ink-800 oklch(0.32 0.020 250)` · `--ink-700 oklch(0.40 0.020 250)`
- `--fog-50 oklch(0.98 0.006 250)` · `--fog-100 oklch(0.95 0.008 250)` · `--fog-300 oklch(0.84 0.013 250)` · `--fog-450 oklch(0.74 0.015 250)` · `--fog-550 oklch(0.65 0.016 250)`
- `--accent oklch(0.82 0.145 215)` · `--accent-dim oklch(0.70 0.11 215)` (ADOPTED — the `--accent`/`--accent-dim` pair replaces the prior `--c-accent-500/400`; `-dim` is the hover/secondary step the mock uses)
- **Semantic status** `--ok oklch(0.78 0.15 152)` · `--warn oklch(0.82 0.13 80)` · `--err oklch(0.66 0.17 25)` (ADOPTED — the `--ok/--warn/--err` semantic names replace the prior `--c-built/soon/danger-500` primitives; status is still label+icon, never color alone — see §Color & contrast)
- **Kind accents** `--kind-svg oklch(0.80 0.13 295)` · `--kind-canvas oklch(0.82 0.13 50)` · `--kind-html oklch(0.80 0.12 175)` (ADOPTED — the interactive-component-kind accent colors the apparatus/figures key off; new vocabulary, no prior §0 equivalent)
- **Generating / source accents** `--pipeline oklch(0.762 0.154 159)` (ADOPTED — the generating-pipeline green: graph/timeline + finding ✓; Figma `built/500 #3ecf8e`, hue 159, DISTINCT from the cooler status `--ok` hue 152) · `--source-link oklch(0.697 0.160 258)` (ADOPTED — the grounded-source link blue; Figma `accent/500 #5b9dff`) · `--faint oklch(0.568 0.029 254)` (ADOPTED — the finest meta/fine-print ink; Figma `fog/500 #6b7888`). The brand cyans the mark gradient sweeps are NOT new tokens — `#00dbfe` resolves (OKLCH-verified) to `--accent` and `#35afc9` to `--accent-dim`.
- **Badge-border tints** `--badge-border-ok oklch(0.499 0.080 152)` · `--badge-border-warn oklch(0.549 0.090 80)` · `--badge-border-neutral oklch(0.401 0.019 248)` (ADOPTED — the dim 1px outline on the library status chips, Figma `6:2` `#3d704c`/`#8d6b2d`/`#404952`; the chip TEXT stays the brighter status/pipeline color, the border is this desaturated step. neutral is also the chrome-control outline.)

**Translucent / gradient surface system** (ADOPTED — the Figma chrome floats over a radial app-bg behind frosted semi-opaque surfaces + thin dividers, transcribed from the `5:2`/`6:2`/`1:2` frames; **CHROME-ONLY** — not injected into the opaque-origin artifact, which has no app cascade to float over). All are §0 raw literals referenced by name in the chrome only.
- `--app-bg radial-gradient(ellipse 190% 96% at 50% -10%, #0f1821 0%, #080f16 55%)` (the top-center radial canvas wash behind every chrome screen)
- `--surface-header oklch(0.205 0.020 250 / 0.6)` · `--surface-panel oklch(0.205 0.020 250 / 0.5)` · `--surface-panel-strong oklch(0.205 0.020 250 / 0.55)` · `--surface-pill oklch(0.215 0.018 250 / 0.6)` · `--surface-ledger oklch(0.205 0.020 250 / 0.55)` · `--surface-divider oklch(0.245 0.020 250 / 0.55)` (the alpha-surface tiers: header/pill 0.6, panel 0.5, panel-strong/ledger 0.55, divider 0.55)
- `--brand-gradient linear-gradient(140deg, var(--accent), var(--accent-dim))` · `--brand-glow 0 0 8px oklch(0.82 0.145 215 / 0.4)` (the mark/avatar gradient `#00dbfe→#35afc9 ≈140deg` reusing the brand cyans, + its cyan halo — also the reading-progress bar glow)

**Semantic** (reference primitives)
- bg: `--bg-app → ink-950` · `--bg-surface → ink-900` · `--bg-raised → oklch(0.215 0.018 250)` · `--border → ink-800` (ADOPTED — the mock raises `--bg-raised` to its own near-ink value and points `--border → ink-800`; the prior `--border → ink-700` is superseded)
- text: `--text → fog-100` · `--text-muted → fog-450` · `--text-faint → fog-550` (muted/faint shift to the new `--fog-450/550` steps; `--faint` is the even-finer fine-print ink below these — a primitive, used directly)
- interactive: `--interactive → accent` · `--interactive-hover → accent-dim`
- status: `--status-built → ok` · `--status-soon → warn` · `--status-error → err`

**Type scale (rem)** `--fs-hero 2.5` · `--fs-h1 1.875` · `--fs-title 1.59375` (25.5px — chrome page title, Figma `6:2` 25px) · `--fs-h2 1.5` (24px — lesson H2 serif; CORRECTED from 1.375/22px to the Figma `3:2` value — see *Intra-Figma type-scale conflict* below) · `--fs-card-title 1.15625` (18.5px — library poster title, Figma `6:2`) · `--fs-lede 1.1875` (19px — lesson lede) · `--fs-body 1.0625` (17px reading body — CORRECTED from 1rem/16px to the Figma `3:2` / v11 `.prose-flow p` value — see *Intra-Figma type-scale conflict* below) · `--fs-small 0.875` (14px) · `--fs-mono 0.9375` (15px) · `--fs-caption 0.78125` (12.5px — gloss def / card description / fine-print) · `--fs-micro 0.625` (10px — uppercase mono eyebrows / kickers)
>
> **Intra-Figma type-scale conflict (`--fs-body` / `--fs-h2` / `--lh-reading`).** The Figma file itself disagrees on these three across two surfaces: the **Foundation/§0 token page** (node `9:15`) reads `Body/Base 16px / lh 1.5` and `Heading/H2 22px` (verified via `get_variable_defs`), while the **lesson-workspace screen frame** (node `3:2`) reads the 17px / 24px / 1.7 reading rhythm transcribed above. §0 reconciles toward the **screen frame `3:2`** — it is the real layout the build targets (its H2 text-node box geometry, ~29px, fits 24px, not 22px), and per the authority chain a disagreeing Figma value is reconciled INTO §0 here, not built from. The Foundation page `9:15` is therefore the **stale Figma surface** for these three tokens — a human should update it to 17/24/1.7 on the next Figma edit so the design-system page matches the screens.
**Letter-spacing** (ADOPTED — new scale; negative on tight display/headings, positive in `em` on the uppercase-mono eyebrows + fine-print so it scales with the §0 caption/micro sizes; Figma tracking values reconciled to em) `--ls-display-tight -0.015em` (hero / serif h1) · `--ls-display -0.01em` (h2 / card title) · `--ls-snug -0.005em` (page title) · `--ls-meta 0.02em` (mono fine-print / card meta) · `--ls-eyebrow 0.16em` (uppercase mono eyebrow / kicker) · `--ls-eyebrow-wide 0.2em` (widest stage-label / topic eyebrow)
**Line-height** (ADOPTED — per-role tokens; supersede the §Typography "1.5 body / 1.2 headings" pair) `--lh-reading 1.7` (reading prose, Figma `3:2` / v11 `.prose-flow p`; the Figma Foundation page `9:15` still reads 1.5 — see *Intra-Figma type-scale conflict* above) · `--lh-display 1.06` (serif display / lesson h1) · `--lh-heading 1.18` (lesson h2) · `--lh-gloss 1.55` (gloss / caption band) · `--lh-card-desc 1.4` (library poster-card 2-line description, Figma `6:2` card node `6:47`: the `6:46` box is 35px ≈ 17.5px/line ≈ 1.40 at 12.5px — TIGHTER than `--lh-gloss` so two clamped lines fit the fixed card body without `overflow:hidden` slicing the second line's descenders)
**Space (rem)** `--sp-1 .25` · `--sp-2 .5` · `--sp-3 .75` · `--sp-4 1` · `--sp-5 1.5` · `--sp-6 2` · `--sp-7 3`
**Radii** `--r-sm 6px` · `--r-md 10px` · `--r-card 12px` (apparatus panel, Figma `1:2` panels rounded-12) · `--r-card-lg 14px` (library poster, Figma `6:2` cards rounded-14) · `--r-lg 16px` · `--r-pill 999px` (user chip / status capsule) · `--r-kbd 4px` (keyboard-hint key cap, v11 mock)
**Geometry** (ADOPTED — the lesson-workspace spine/panel/frame metrics §Lesson layout depends on; new vocabulary, no prior §0 equivalent)
- `--measure 33rem` (~62ch frozen reading spine — the HARD stable-spine invariant) · `--panel-w 23rem` (the apparatus panel, ~22–26rem) · `--col-gap clamp(1.6rem,2.6vw,3.4rem)` (the reading↔panel gutter)
- `--edge-gap clamp(1.6rem,2.4vw,3.2rem)` (equal outer gutters flanking the workspace — never lopsided) · `--scrub-w 1.1rem` (the dot-scrubber's grid track — now VESTIGIAL: the shipped scrub rail is a `position: fixed` right-edge-pinned cluster per Figma 3:1120, see §Lesson layout PR-C) · `--frame-max 1640px` (the ultra-wide cap bounding the whole reading+gap+panel+scrub assembly, ~1500–1640px)
**Motion** `--dur-fast 120ms` (unchanged) · `--dur-base 200ms` (ADOPTED — was 220ms; resolves the §0↔mock conflict to the mock's snappier base) · `--dur-slow 440ms` (ADOPTED — was 360ms; the mock's longer slow tier for the card↔reader morph) · `--ease cubic-bezier(.2,.75,.2,1)` (ADOPTED — unified to the mock's single ease, superseding the prior split `--ease-out`/`--ease-in-out` pair; one ease keeps motion coherent across the catalog)
**Type families** `--sans var(--font-inter),ui-sans-serif,system-ui,-apple-system,"Segoe UI",sans-serif` (CORRECTED — leads with the LOADED Inter, the Figma sans proxy, via `next/font` `--font-inter`; falls back to system UI sans) · `--mono "SF Mono",ui-monospace,var(--font-jetbrains-mono),"JetBrains Mono",Menlo,monospace` (CORRECTED — the SF-Mono / JetBrains-Mono split kept as ONE token: SF Mono where present (Apple, the Figma eyebrow/fine-print face), the LOADED JetBrains Mono (`--font-jetbrains-mono`, the Figma timeline face) guaranteeing the mono voice on Linux / Cloud Run) · `--serif "Iowan Old Style","Charter",Georgia,"Times New Roman",Times,serif` (CORRECTED — explicit Linux/Cloud-Run fallback chain added, no webfont; lesson headings only — see §Typography). The opaque-origin artifact's injected copy (`serve.ts`) drops the `var(--font-*)` entries the sandbox can't resolve and falls to the system faces (`serve.test.ts` normalizes that one divergence). Fonts are loaded at the app boundary (`src/app/layout.tsx`), not in core code (§Typography).

## Color & contrast
Dark, technical palette (inspired by the ai-concept-viz explainers). Body `--text` on `--bg-app` clears WCAG AA for normal text; muted text used only at ≥ `--fs-body`. Status is always conveyed by **label + icon**, never color alone. Target: WCAG 2.2 AA.

## Typography
Three families, all declared in §0. Fonts are **chrome**: Inter (`--sans`) and JetBrains Mono (`--mono`) are LOADED once at the app boundary via `next/font/google` in `src/app/layout.tsx` (which puts `--font-inter` / `--font-jetbrains-mono` on `<html>`, picked up by the §0 stacks) — never imported in core code; `next/font` self-hosts the files and emits a metrics-matched fallback with `display: swap`, so the chrome shows real Inter without a layout-shift (FOUT, not CLS). **Chrome + body** use the sans stack (`--sans`, leading with loaded Inter): the intake form, top bar, library, generating-state — everything that isn't a lesson heading. **Lesson headings** (the lesson `h1`/`h2`) use the serif stack (`--serif`, ADOPTED in TS-3 from the v11 mock; its explicit Georgia→Times fallback keeps a serif voice on Linux/Cloud Run where the Apple reading serifs are absent) — a reading voice distinct from the chrome's sans. **Code/tokens, eyebrows, fine-print, and the generating timeline** use the monospace stack (`--mono`, ONE token spanning the SF-Mono / JetBrains-Mono split — Figma uses SF Mono for eyebrows/fine-print and JetBrains Mono for the generating timeline; the loaded JetBrains Mono guarantees the mono voice off-Apple); the per-step timer/durations use it with tabular figures so digits don't jitter (§Components → Progress). Scale per §0. **Line-height is per-role** (§0 `--lh-*`, superseding the old single "1.5 body / 1.2 headings" pair): `--lh-reading 1.7` for reading prose, `--lh-display 1.06` / `--lh-heading 1.18` for the serif lesson headings, `--lh-gloss 1.55` for the gloss/caption band. **Letter-spacing is per-role** too (§0 `--ls-*`): tight-negative on display/headings, positive on the uppercase-mono eyebrows + fine-print.

## Motion
Transitions use the **`transitions-dev`** snippet catalog (copy-paste CSS, no runtime dependency) at the §0 durations and the single §0 `--ease` (the `--dur-fast`/`--dur-base`/`--dur-slow` tiers + `--ease`; this section states no literal duration of its own — §0 wins, and the motion catalog consumes the resolved 120/200/440 + `--ease`). **The catalog is LANDED (TS-24):** `src/app/globals.css` `:root` declares a small set of NAMED transition primitives (`--tr-color`/`--tr-bg`/`--tr-hover` @ `--dur-fast`, `--tr-progress`/`--tr-state` @ `--dur-base`) that compose ONLY `var(--dur-*)` + `var(--ease)` — so EVERY animated surface references a primitive by name and the value lives in exactly one place (no per-component duration/easing literal). Surfaces on the catalog: the library card hover (`--tr-hover`), the primary button + sign-out (`--tr-bg`/`--tr-color`), the reading-progress fill (`--tr-progress`), the generating rail's state change (`--tr-state`); the loop spinner reuses the `--dur-slow` tier with `linear` (a continuous rotation can't ease its loop seam); the card→reader morph reads `--dur-slow`/`--ease` in its View-Transition block. Allowed in the skeleton: staggered hub-tile reveal, the **generating timeline's staggered step reveal** (each rail row enters via the `rail-reveal` keyframe @ `--dur-base`, delay = row-index × `--dur-fast`), modal/panel open, status-badge change, progress updates. The **card→reader box-FLIP morph** is the one exception that rides a different transport: the cross-document `@view-transition { navigation: auto }` mechanism, which activates ONLY on a real full-document navigation — so the FLIP-origin card MUST link via a plain `<a>` (NOT `next/link`, whose client-side soft nav would never fire it; §Components → Library). The **create-form flow** (§Components → Intake form) reuses the SAME morph vocabulary on the SAME-document `/` route via a SCRIPTED `document.startViewTransition` (the card→reader morph is CSS-driven; this flow is same-document so it must be scripted), gated by the `vtOff()` capability+preference guard (`src/app/library-morph.ts`): (a) the **`+New` card → intake form** container-transform shares a `view-transition-name: new-surface` + the `.morph-box` class on both endpoints — the card box geometry-FLIPs into the form at `--dur-slow`/`--ease` (the `::view-transition-group(.morph-box)` "last thing moving" cohesion tier), then the fields **stagger in** a beat after the box settles via the SAME `rail-reveal` keyframe @ `--dur-base`, delay = field-index × `--dur-fast`; and (b) the **submit handoff** is the typed `begin-generate` transition — the typed topic TEXT lifts form→generating-header as a shared element (`view-transition-name: specimen-topic`, running at the `--dur-slow` group tier so it outlasts the root fade) while the OLD (form) root RECEDES via `@keyframes vt-recede` (a fade + slight settle-back @ `--dur-base`), keyed to `html:active-view-transition-type(begin-generate)` so the recede runs ONLY on submit, not the open/close card↔form morph. No new §0 token — every value resolves to a §0 `--dur-*`/`--ease` or a catalog `--tr-*` primitive. **Reduced motion:** `@media (prefers-reduced-motion: reduce)` removes non-essential transitions and staggers (it zeroes `animation-duration` AND `animation-delay`, so the timeline reveal AND the create-form field stagger show instantly with no stagger) AND the `vtOff()` guard skips every scripted `startViewTransition` so the create-form swaps are instant (synchronous, no morph/recede); the create flow's typed-root VT CSS is itself scoped under `prefers-reduced-motion: no-preference`; status/progress change instantly.

## Components (skeleton surfaces)
- **Library** (`/`, the home) — REBUILT to the Figma library frame `6:2`: an owner-scoped **card grid** of the signed-in user's lesson posters and the product's **sole generation entry** (the intake form is the grid's FIRST cell — a `+ New lesson` card that grows in place into the form; see §Components → Intake form, the create-form flow). Scoped under `.library*` (the page body) — distinct from the shared `.appbar*` header chrome and the chromeless `.signin*` scope, so it neither leaks nor collides. The `.library__head` section row carries the serif "Lessons" `.library__title` (the `--serif` stack, `--fs-title`, in `--fog-50` — Figma `6:18` 25px) and a right-aligned mono `.library__hint` ("Tap a built lesson — the card opens into the workspace.", `--text-faint`, Figma `6:21`). Then a `.library-grid` (FIXED 291px columns matching the Figma `6:2` card wall — node `6:25` is 291.5px — LEFT-justified via `justify-content: start` so the cards keep their fixed size and CLUSTER LEFT, like the frame's tight wall with empty space at the right, rather than a `1fr` stretching each to full-bleed; `auto-fill` reflows the fixed columns down to one on mobile) over `listLessons`. Each **poster card** (`.library-poster__card`, Figma `6:25`: `--bg-surface` over a 0.8px `--border` outline, `--r-card-lg` 14px corner) is a bounded box — the card→reader morph's **FLIP origin** (TS-21 names + animates it via a per-card `view-transition-name`; TS-17 sets the endpoint but runs no animation, box-only per the TS-5b verdict) — linking cross-document to `/lesson/<id>` (a plain cross-document `<a>` anchor — NOT `next/link`, whose soft nav would never fire the cross-document View-Transition; no SPA shell). Each card is FIXED at the frame's **262px card height** (Figma `6:25`) so the body bottom-pins its footer — the frame's title-top / footer-bottom rhythm. It stacks a 104px **poster wash** (`.library-poster__wash`, Figma `6:26`: a subtle radial tint over `--bg-surface`, divided by a fine `--ink-850` rule, carrying the decorative `<PosterMark/>` node-graph motif in `--text-faint`) above a `.library-poster__body` (a flex column, the head block pinned top + footer pinned bottom via `justify-content: space-between` over the fixed height). The DENSE card (matching the full Figma `6:2` card) fills the body's rows top-to-bottom: a `.library-poster__head` block (6px-gap column) holding the subject **EYEBROW** (`.library-poster__eyebrow`, Figma `6:41` — uppercase mono `--fs-micro` in `--warn`, `--ls-eyebrow`), the serif **TITLE** (`.library-poster__title`, `--fs-card-title` in `--fog-50`, two-line clamp), and the one-line **DESCRIPTION** (`.library-poster__desc`, Figma `6:47` — sans `--fs-caption` in `--text-muted`, two-line clamp); then a bottom-pinned `.library-poster__foot` row: a status `.badge` LEFT (label + icon: Built/Soon/Text) and the **`level · depth · time` meta** RIGHT (`.library-poster__meta`, Figma `6:57` "beginner · d2 · 3h ago" — mono `--fs-micro` in `--text-faint`, `--ls-meta`). **Every value is REAL stored data, never fabricated.** The eyebrow is the subject category (BIOLOGY / MATHEMATICS / …) derived by the **isolated, FAIL-SAFE category classifier** — a separate cheap analysis-tier helper (`src/pipeline/classify-category.ts`) run at the `runLesson` TAIL, never wired into the core stages and never touching the `LessonBrief` contract; any classifier fault yields `category = null` and the lesson is unaffected. The description is the lesson's learner-facing one-liner (`summary` = the brief's `learningGoal`, pure data plumbing — no extra generation). The meta line is `level` + `depth` from the request's saved `settings_json` Settings (`metaLine`, `intro` surfaced as the learner word "beginner") + the relative time from `createdAt`. `category` + `summary` are NULLABLE `curriculum` columns: a NULL value (an old/legacy row, or a classifier miss) OMITS that row entirely in `page.tsx` — no empty band, the rhythm stays tight — and the read side re-validates the stored category (`categoryEyebrow`) so a leaked code identifier or generic filler is shown as nothing (show nothing > leak/guess), the copy-appropriateness gate. The library tolerates mixed blob/v11 + degraded + legacy-NULL rows (no card crashes the grid). Zero lessons is never an empty grid — the `+ New lesson` create card is always the first cell (the create-form flow superseded the prior `.library-empty` first-run prompt; the `.library-empty*` and the prior always-open `.library-intake*` styles are now unused). The grid that holds the `+New` cell + the poster cards is `.lessons-grid` (the create island `library-create.tsx` renders it; the server `page.tsx` passes the poster-card `<a>`s in as children). Sourced from §0 tokens. The card box is the morph's FLIP origin (TS-21).
- **Intake form** (the create-form flow) — topic field + settings (level, depth, audience); submit triggers a generation run. It is the product's sole generation entry, hosted on the **Library** home (`/`) as the FIRST cell of the card grid: a dashed `+ New lesson` card (`.newcard` — a `--border` dashed box matching the poster card's 262px height, with a `+` glyph in `--interactive`, a "New lesson" label, and a mono "start a run" sub-label). Clicking it (or pressing the global `n` shortcut) GROWS that cell in place into the intake form — a same-document container-transform (the `+New` card and the form share `view-transition-name: new-surface` + `.morph-box`; §Motion), the form spanning the full grid row (`.lessons-grid--form-open .lessons-grid__create`) while the poster cards reflow beneath. `Esc` / the "Esc · Cancel" control (`.intake__close`) collapses it back (the morph in reverse). The form (`.intake`, a `--bg-raised` card lifted to a `--interactive` active border, `--r-lg`, `--sp-5` padding) holds the header row (`.intake__head`: a serif "New lesson" `.intake__title` + the Esc·Cancel control), the four `.field*` controls (Topic, then a Level select + Depth slider side by side at ≥520px via `.field-row`, then optional Audience, then the Generate `.btn`), the eased-on-appearance error chip (`.intake__error` + the `--show` modifier, via the `--tr-state` primitive — status by `role="alert"` text + `!` icon, never color alone, §Accessibility), and the `.intake__note` ("Runs on Haiku, capped — about a minute and a few cents."). Fields stagger in via `rail-reveal` (§Motion). The field `.field__input` border eases toward `--interactive` on focus via the `--tr-hover` catalog primitive while the `:focus-visible` ring stays instant (a11y). On a successful submit (`POST /api/generate { topic, level, depth, audience }` → `202 {id}`, surfacing 400/401/403/502 as the error text — the contract is UNCHANGED) the form RECEDES and the typed topic text LIFTS into an in-place generating shell (`.generating-shell` on `/`: the shared live-research generating view — the carried-forward topic in `--interactive` via the `specimen-topic` shared element, the stage stepper, the research node-graph + LIVE RESEARCH panel, and the compact progress pill; §Components → Generating / Progress + §Motion's `begin-generate` typed transition), which polls status and navigates to `/lesson/<id>` once the run lands. The `+New` card being the first cell means a fresh account is never an empty grid — the create affordance IS the first card. The client island is `src/app/library-create.tsx`; the pure motion guard + the shared name constants are `src/app/library-morph.ts`.
- **Generating / Progress** — the **live-research generating view**, REBUILT (superseding the Figma `1:2` side-rail "B view" #154) to the owner-approved, measured-sound **FULL-WIDTH, COLUMN-LOCKED TABLE** (the design deliverable in `.superpowers/generating-layout/SPEC.md` + `prototype.html`). One SHARED component (`src/app/lesson/[id]/generating-view.tsx`, scoped under `.gen*`) rendered by BOTH entries so they never diverge: the reader route `/lesson/<id>` while a run is in flight (`generating.tsx`'s poller) AND the create-form's in-place generating shell on `/` (`library-create.tsx`, hosted in `.generating-shell`). It is a single-lesson generating state laid out TOP-TO-BOTTOM: a **top chrome band** (`.gen-top` — a wordmark + a live phase shimmer derived from the running stage; in the pre-`plan` **dispatch window** — the run has been dispatched but its cold-starting Job hasn't emitted a first step yet — it reads a leading **"Starting…"**, issue #162, which resolves to the running stage's label the instant a real step lands) → a `.gen-topic` header (an optional subject eyebrow → "Generating <topic>…" → an optional `<level> · depth <n> · building one lesson` settings sub-line) → the **TABLE** (`.gen-table` — the stepper column-headers over a column-locked plane, then the full-width research band) → a **compact progress bar** (`.gen-progress`) → a bottom **state legend** (`.gen-legend`). **The topic** is shown only where it's known — the create-form path carries the typed topic (it morphs in via the `specimen-topic` shared element, `.gen-topic__topic` in `--interactive`); the reader-route refresh path has NO topic pre-persist (the run isn't persisted; the `run_owner` poll-window stamp carries no topic — not the pipeline's to surface), so the header honestly degrades to a bare "Generating…". **The settings sub-line** (`.gen-topic__settings`, the run's `level` + `depth`) is likewise shown only where those values are known to the caller: the create-form path passes them from the form state; the reader-route refresh path omits the sub-line. **The subject eyebrow** (`.gen-topic__eyebrow`, uppercase mono `--accent`) is the run's REAL category, classified at the run TAIL (`classify-category.ts`) and therefore NOT in the live poll — so it is **omitted on every live path today**, never fabricated; it is wired for a future caller that can truthfully supply it. The client polls run status (the one-page curriculum appears atomically when the run completes).
  - **THE COLUMN LOCK (the load-bearing invariant).** The **stepper** (`.gen-stepper`, the six column headers) and the **plane's node grid** (`.gen-plane__grid`) share ONE CSS Grid track set — `grid-template-columns: repeat(var(--gen-cols), 1fr)` with one `column-gap` — so a header in grid-column `i` and a node cell in grid-column `i` are X-centered on the SAME track center: a node's x-center == its header's x-center **BY CONSTRUCTION** (Δ0px, measured; no JS x-math, the alignment can't drift because the platform owns it). The six `1fr` tracks divide the **FULL content width** (no right rail) — the table reads as columns that ARE the pipeline phases (Plan · Research · Brief · Spec · Code · Critic). `--gen-cols`, `--gen-col-gap`, `--gen-plane-h`, `--gen-node-*`, `--gen-col-pad`, `--gen-bend`, `--gen-research-*` are **component-local custom properties** scoped to `.gen` — NOT §0 tokens (component metrics, like the lesson workspace's panel internals), so this is **ADDITIVE CSS ONLY: no §0 retoken**, no manifest-copy edit, no `serve.test.ts` guard trip.
  - **Stepper (the column headers)** (`.gen-stepper`) — the six pipeline stages as the column headers: each a mono `.gen-step__label` + a `.gen-step__bar` under-bar whose color/weight is the current-phase indicator + a `.gen-step__count` line (Research shows `done/N`). State (ran/running/pending) reads by the under-bar color paired with the label brightness + a visually-hidden state word — **never color alone** (§Accessibility): solid `--pipeline` = ran, `--accent` (4px) = running, `--border` = pending. Folded from the SAME `deriveRail` six-stage list (`stage-rail.ts`) that drives the progress bar. **NO graph** pipeline stage (the graph PATH is dormant, decision 9 / ADR-0003 — surfacing it would fabricate a phase).
  - **The plane + the research fan** (`.gen-plane`, height reserved at `--gen-plane-h` via `contain:layout`) — one column CELL (`.gen-cell`, a flex column centered) per phase. Plan/Brief/Spec/Code/Critic are single nodes at a fixed uniform `--gen-node-single-h` (the spine reads as one row); Research is the in-column FAN — N nodes distributing symmetrically about the column center. Each node card (`.gen-node`, a `[label] [title] [meta]` shared row set) encodes state by **border style + glyph + sr-word, never color alone**: solid `--pipeline` + `✓` = ran, dashed `--accent` + `⟳` = running, dotted `--faint` + `○` = pending. The `.gen-node__title` (`-webkit-line-clamp: 2`) lives in the grid's `1fr` middle track but is `align-self: start` + capped at `max-height: 2 × the 1.3 line-height`, so the clamp box keeps its own ≤2-line content height instead of being stretched by the track to a fractional line count — `overflow: hidden` then ALWAYS clips on a clean line boundary, never a sliced partial line bleeding above the meta (component-local, no §0 retoken). **THE FIT-MATH (the column always fits, SPEC §2.3, the pure `fit-column.ts`):** node height is a pure function of column height and count (`fitColumn(colH, count, gap, minH, maxH, chipH)`), budgeted against the inset `colH = planeH − 2·--gen-col-pad` with the overflow-chip band reserved — so the Research stack NEVER overflows at any N (3 visible @ ≤112px at the documented tokens; N>3 overflows the remainder DOWN into the research band with a `+K below` chip). The plan/brief node states come from the rail; each research node's state + question title + finding-count detail come from the live feed (`research-graph.ts`). An empty feed degrades to the honest `PLAN→BRIEF` spine — never a fabricated node (AGENTS.md anti-invention).
  - **Edges** (`.gen-plane__edges`, an SVG layer BENEATH the nodes) — the DAG `plan→rᵢ`, `rᵢ→brief`, then the linear spine `brief→spec→code→critic`, as cubic béziers with horizontal tangents (control handles at half the inter-column gap, min `--gen-bend`). Every endpoint is read from a MEASURED node rect (`getBoundingClientRect` − the plane rect → plane-local px) in a layout effect + a `ResizeObserver`, so it lands exactly on the node border at every size/count (SPEC §3). The draw is **gated on the entrance settling** (SPEC §3.5): a node mounts with the `rail-reveal` enter (`translateY(--sp-1)` → 0), so a measurement taken DURING the entrance would anchor every edge ~4px below center; the draw waits for the node's `rail-reveal` `animationend` (with a double-rAF fallback that also covers the reduced-motion zeroed-duration path), and a monotonic token makes a deferred draw idempotent against a fast re-render. A settled edge (source ran) draws solid in `--pipeline`; a pending one stays dashed `--surface-divider`. The five geometry guarantees (column-lock, research margins, spine uniformity, **edge anchors ≤1px after the entrance settles** — SPEC §10.4 — plus **title-clamp integrity**: every node title clamps to an integer ≤2-line count, never a sliced partial line) are CI-asserted against the BUILT app by `e2e/generating-geometry.spec.ts` (the first four at 1440×900 for N∈{3,8}; title-clamp at BOTH viewports over short/medium/long titles). On mobile the plane is block-flow and the SVG hides (stacked nodes need no crossing connectors).
  - **Live-research band** (`.gen-research`, a FULL-WIDTH band directly below the plane — the relocated evidence surface, SPEC §6.B) — a pulse dot + `LIVE RESEARCH` + an "N / M extracted" count over a `repeat(auto-fill, minmax(--gen-research-card-min, 1fr))` card grid (grows in 2D, wraps into bounded rows, scrolls internally past ~`--gen-research-rows` so the fixed-height plane above never moves). It carries (1) the grounded findings as evidence cards — a claim (a `✓` `--pipeline` once landed, a `⟳` `--accent` while extracting) + an em-dash source HOST in `--source-link` (**copy-safe**: `research-graph.ts` reduces a URL to its bare display host, never a raw URL or an internal id, falling back to the title then to nothing) — and (2) the **research OVERFLOW SINK**: research questions beyond the column cap render as QUEUED dotted `○` cards carrying their `R{n}` label + a "queued" sub-label. Empty/parked (no findings AND nothing overflowed) shows a single muted row, never a conspicuous void. Findings stagger in via the catalog `rail-reveal` (zeroed under reduced motion).
  - **Progress bar** (`.gen-progress`, under a `Pipeline progress` label) — the run's pipeline as ONE **compact inline list** of `stage Xs glyph` segments, dot-separated: `plan 2.1s ✓ · research 11.4s ⟳ · brief — · spec — · code — · critic —` (the six live stages, lowercase mono; **NO graph** stage). All six show from the first paint, each conveying state by a glyph (`✓` done · `⟳` running · `—` pending · `✗` failed) **plus** a visually-hidden state word and its timing: a not-yet-started stage shows an em-dash; the in-progress stage a **live ticking elapsed timer** (`--accent`) that freezes into a duration once the next poll reports it finished; a finished stage its frozen wall-clock duration; a failed stage's name + glyph `--status-error`. A single mono `.gen-progress__caption` line names the in-progress stage with its live timer (reading **"Starting…"** in the pre-`plan` dispatch window — issue #162, no timer there since no stage runs yet; falling back to "Working…", or a "still working" hint when the poll stalls). Timers use the monospace stack with tabular figures so digits don't jitter (§Typography). The SAME real per-step data the prior surface carried — folded over `deriveRail` (`stage-rail.ts`).
  - **Code-phase progress bar** (`.gen-codebar`, PR-4 / issue #180) — a learner-safe "Writing the lesson…" progress bar rendered INSIDE the **Code column node** while the `code` phase streams (the longest phase, ~83% of run wall-clock, otherwise opaque). A small mono label (`.gen-codebar__label`, `--accent`) over a thin `.gen-codebar__track` whose `.gen-codebar__fill` width is the live **fraction** — state by **LABEL + bar, never color alone** (§Accessibility), and a `role="progressbar"` with `aria-valuenow` = a rounded "how far along" percent for AT. **BAR ONLY — no numeric readout**, and crucially **no token count / cost / model / cap** anywhere on the surface (the fraction is computed `outputTokens ÷ maxTokens`, clamped ≤0.95, **in the sink**; only `{ fraction, elapsedMs }` ever reach the client — §"No project internals in user UI"). The ~0.95 clamp prevents a false "done" before the critic gate; the bar is rendered ONLY while the `code` rail stage is `running`, so it **disappears the instant `code` flips to done** (a stale row is then inert). It lives in the node's fixed-height grid (`overflow:hidden`), so it can't change the node's outer height or center → the column-lock + spine-uniformity geometry is unchanged. The fill transition rides the §0 catalog (`--dur-base`/`--ease`), zeroed under `prefers-reduced-motion` by the global guard; **no JS animation**. Driven by the new `code` poll field (see Data path). Component-local CSS — **no §0 retoken**.
  - **State legend** (`.gen-legend`, the bottom row) — `solid — ran · dashed — in progress · dotted — pending`, each a short sample bar (`.gen-legend__bar`) in the matching border style: it tells the reader how to read the graph nodes + the stepper under-bars BY STYLE (the same vocabulary the §Accessibility rule mandates). Mono fine-print, decorative bars `aria-hidden`.
  - **Motion (SPEC §7).** All motion is the §0 catalog only — the node + finding `rail-reveal` staggered enter (@ `--dur-base`/`--ease`, delay = index × `--dur-fast`), the state-change `--tr-state` primitive (the stepper under-bar, an edge), the edge's compositor-friendly draw, and the live-phase + research-pulse loops (rebound to the §0 `--dur-*` tiers); **no JS animation lib**. Under `prefers-reduced-motion` the global guard zeroes every duration AND delay so the layout shows instantly with no stagger (§Motion) — the layout itself is unaffected (motion is decorative chrome). These chrome motions live in `globals.css` + the React chrome — never inside the sandboxed lesson iframe (its own motion/a11y contract).
  - **Responsive (SPEC §9).** At ≤60rem the table collapses to a single column: the stepper folds to a vertical phase list; the plane becomes normal block flow (each phase a stacked block, all research nodes shown — no cap); the SVG edge layer hides; the research band's card grid collapses to one column with its max-height cap released. No horizontal overflow at 390px.
  - **Data path (cite).** All the surfaces are driven by the EXISTING owner-scoped status poll `GET /api/lesson/[id]/status`, which since the **live-research data path (Stage 1, #153)** + the **code-phase bar (PR-4 / #180)** serves FOUR fields behind one `ownsRun` gate: `steps` (the per-step timing — `step_event`, issue #61) feeds the stepper + the progress bar + the per-column node STATE (done/running/pending); `research` (the per-question feed — `research_event`: the planned questions + each question's grounded `{claim, url, title}` findings/sources, owner-scoped + copy-safe, pruned at persist) feeds the Research column question nodes + the live-research band's findings + the overflow queue; `code` (the live code-phase progress — `code_progress`: a learner-safe `{ fraction, elapsedMs }`, the fraction computed + clamped IN THE SINK so no raw token/cost/model crosses the wire, owner-scoped, pruned at persist) feeds the Code-node progress bar; `ready` triggers the route refresh. The `code_progress` write is **fail-safe + fire-and-forget** (the `PgCodeProgressSink` mirrors `PgResearchSink`: throttled, never awaited on the critical path, every write self-wrapped), so a write fault never slows or breaks the paid `code` stream — the bar simply degrades to the running-code elapsed timer. **This generating view is the LIVE, in-flight surface — replaced by the lesson artifact (or the degraded state) the instant the run persists; but it is no longer the ONLY reader of `step_event`.** As of issue #175 `step_event` is KEPT past persist (only `research_event` / `step_result` / `run_owner` are still pruned) to power a FROZEN owner-only sibling on the finished page — the **Build details** disclosure (below), which replays the same six-stage rail at rest. The live view drops the `LiveTimer`; the frozen sibling shows frozen durations only. The header's `topic`/`level`/`depth`/`category` are NOT in the poll — the create-form path passes `topic`/`level`/`depth` from its form state, and `category` is omitted everywhere live. No new data path, no durable store, no pipeline change, and the opaque-origin lesson iframe + its trust boundary are never touched. DESIGN.md wins on any design conflict (authority chain §0).
- **Single lesson** (`/lesson/<id>`) — the read route for a completed single-lesson run. A `built` lesson renders in the **reader shell** (`.reader.reader--ws`): the integrated **topbar** (`.ws-topbar`, PR-D — the back link + centered wordmark + ⌘K/⇧F chord + the user pill, with the reading-progress HAIRLINE at `y=0` driven by the posted `scrollProgress`; it suppresses the global `.appbar` via `body.has-ws-topbar`), then the standalone framing (the parent topic as eyebrow → the lesson title as `h1` → `.lead` `level · depth N`, in `.reader-head`), then the **lesson-workspace grid** — the `[read]` track holds the lesson's interactive artifact in the sandboxed `.artifact-frame` (`sandbox="allow-scripts"`, no `allow-same-origin`) wrapped in the `#readerPanel.morph-box` card→reader FLIP destination, the `[panel]` track the apparatus stack (the where-am-i widget + the posted section list), the `[scrub]` track the section dot-rail. **Focus-reading** (Shift+F / the topbar toggle) hides the panel + scrub and re-centers a wider reading spine. When the run produced a non-`built` (`soon`/`text`) lesson, a labeled degraded state (`.lesson-degraded`: a status `.badge` with label + icon + a `.lead`) replaces the shell (under the global `.appbar`, no integrated topbar) — never a blank iframe, no morph-box, no progress.
- **Reading progress** (`.reading-progress`) — the reader-shell progress affordance: a slim `.reading-progress__track`/`__fill` bar plus a `.reading-progress__label` percent readout. The fill width is driven by the `scrollProgress` scalar (`0..1`) the lesson posts over the decision-12 `postMessage` channel; before any valid message (or for a lesson that posts none) it reads 0%, so the shell is fully usable over a bare iframe. Status is legible by NUMBER (the tabular-mono percent), never color alone (§Color & contrast). The fill transitions at `--dur-base`/`--ease`; under `prefers-reduced-motion` the fill updates instantly (§Motion). The chrome reads only posted coordinates — never the opaque-origin iframe's DOM.
- **Build details** (`.build-summary`, OWNER-ONLY — issue #175) — a quiet "How this was built" disclosure on the persisted lesson page, for the owner (who in this private-per-user app is also the learner). A native `<details>` whose collapsed Tier-0 summary reads `How this was built · built in Xs · N steps · ✓ passed` (BUILT) or `See what happened · couldn't finish · ✗ not built` (DEGRADED), expanding to the **FROZEN six-stage rail** — one row per pipeline stage with a **learner-safe label** (Planning · Researching · Drafting · Designing · Building · Reviewing — NEVER the raw `plan`/`spec`/`critic`/`code` identifiers), a status **glyph + visually-hidden state word** (✓ done · ✗ didn't finish · — didn't run, status by label + icon never color alone, §Accessibility), the **frozen per-step duration**, and a small **state legend**. **TIMELINE-ONLY:** NO token/cost/model/TTFT/ms anywhere (that deep data lives in the dashboard + eleatic, never a reading surface); it replays the durable `step_event` timeline (kept past persist — issue #175 — see §Components → Generating). The summary `✗` reflects the lesson didn't build; a per-STAGE `✗` appears ONLY where a step actually threw (`step_event.status='error'`) — a graceful critic-vapid / coverage degrade shows every step `✓` under a degraded summary. **Owner-gated for free by co-location:** server-rendered under the page's existing `getLesson(id, sub)` filter (no new route, no `ownsRun` — `run_owner` is pruned at persist), so a foreign id 404s before render. The ONLY interactive target is the `<summary>` (rows inert); it rides the §0 catalog `--tr-state` expand + a `:focus-visible` ring, aligns to the reading spine (`--frame-max`/`--edge-gap` via `--measure`), and is reduced-motion-friendly (the chevron rotate zeroes under the global guard). Placement: **quiet near the reader head** on the BUILT branch (slotted into `reader-shell.tsx` under `.reader-head`); the **higher-intent "See what happened" entry** on the DEGRADED `soon`/`text` branch. **NOT on the library card.** Component-local CSS — NO §0 retoken. Pure render model in `build-summary.ts`; view + async DB read in `build-summary-view.tsx`.
- **Curriculum hub** (tiered SITEMAP — DORMANT) — tier → category → page tiles; built tiles link to the page, `soon`/`text` tiles are muted with a status badge. Built by `tileView` + the tiered CSS (`.hub`/`.tier`/`.tiles`/`.tile*`/`.category*`); retained for the future curriculum-wrapper milestone (decompose → N lessons) but **not** rendered by `/lesson/<id>` today, which renders the single lesson directly.
- **Page tile** (DORMANT — part of the tiered hub above) — title + status badge (`--status-built` / `--status-soon` / text).
- **Artifact frame** — sandboxed cross-origin iframe; the chrome supplies only the frame (a "report a problem" affordance is deferred).
- **Sign in** (`(auth)/sign-in`) — rebuilt to the Figma frame `5:2` (card `5:5`): a viewport-centered ~380px `.signin__card` on the §0 radial-glow `--app-bg`, scoped under `.signin*` so its look does NOT leak to the library (`.library*`) or lesson routes (the lesson read route keeps the shared `.wrap`/`h1`/`.lead`; the library and intake carry their own `.library*` scope). Top to bottom: the `.signin__badge` (54×54, 0.8px `--accent-dim` outline) holding the ✦ sparkle mark inlined as the `<SparkleMark/>` SVG (`fill="currentColor"` ← the badge's `color: var(--accent)`, so the cyan stays a §0 token — never a remote asset, and never the literal "✦" character, which isn't in Inter and fell back to a per-platform system glyph that mis-sized vs. Figma and drifted dev↔Cloud Run) → the two-tone `.signin__wordmark` ("topic·" Inter SemiBold in `--text` + "synthesis" Inter Black in `--accent`) → the serif `h1.signin__title` ("Sign in to your lessons", the `--serif` stack in `--fog-50`) → the `.signin__lead` copy ("Generate interactive, source-grounded lessons from any topic. Private to your account." in `--text-muted`) → a near-white `.signin__google` pill (`--fog-50` bg, `--ink-950` label, the official 4-color Google G inlined as the `<GoogleG/>` SVG — never a remote asset). The card ends after the button — no footer copy (developer-facing meta like ADR/allowlist notes must never appear in user UI). A rejected / non-allowlisted account surfaces a branded `.signin__error` (`--status-error`). The Google consent popup is the one external surface; every color resolves to a §0 token (the Figma hexes are OKLCH-verified equal — `#00dbfe`→`--accent`, `#35afc9`→`--accent-dim`, `#ebeff4`→`--text`, `#f5f9fc`→`--fog-50`, `#a4acb4`→`--text-muted`), with a few sign-in-specific size/tracking literals (e.g. the 27px serif title) the §0 scale doesn't define.
- **App header** (`.appbar`, the signed-in top-bar chrome — Figma library frame `6:2` node `6:6`) — a frosted 54px bar on the library route (and the degraded reader state), scoped under `.appbar*` (distinct from `.library*` and `.signin*`). On the **built reader route** the integrated `.ws-topbar` (§Lesson layout, PR-D) REUSES this `.appbar` look + tokens and REPLACES the bare bar (the global `.appbar` is suppressed under `body.has-ws-topbar`). Left: the two-tone `.appbar__wordmark` ("topic·" Inter SemiBold in `--text` + "synthesis" Inter Black in `--accent`, 15px, Figma `6:8`). Right: the `.appbar__chip` user pill (`--surface-pill` over a 0.8px `--badge-border-neutral` outline, `--r-pill`) holding the `.appbar__avatar` (a 24px `--brand-gradient` square showing the account initial in `--ink-950`, Figma `6:11`), the `.appbar__name` friendly display name (`--text-muted`, Figma `6:14`), and the `.appbar__signout` quiet text control. Colors resolve to §0 tokens 1:1 (the Figma hexes are OKLCH-verified equal — `#0b1219@0.6`→`--surface-header`, `#10171e@0.6`→`--surface-pill`, `#ebeff4`→`--text`, `#00dbfe`→`--accent`, `#a4acb4`→`--text-muted`), with a few header-specific size literals (the 54px bar, the 24px avatar, the 15px wordmark) the §0 scale doesn't define. Shown only when signed in, so the sign-in page stays chromeless.

## Lesson layout (LOCKED — the design target / acceptance bar)

The **LOCKED** design target the generated single-lesson page is meant to be built against — the acceptance bar a future workspace-rendering pass builds toward (the v11 graded critic that once gated the artifact on this bar was REVERTED to the binary blob critic — see `docs/plans/lesson-workspace.md` — so today no pipeline stage emits or gates against this layout; the live binary `critic` judges generic teaches-goal/interactive/a11y/self-contained, NOT this workspace layout). This section wins on any lesson-layout conflict and is the single source of truth (it supersedes the scratch ledger, now promoted here — see below). The reference implementation is the scratch mockup `.superpowers/topic-synthesis-lesson-workspace-v11.html`. The bar legitimately survives the revert as the design target a future pass renders to; *regression vs. the best prior version* would be an **offline** eleatic `--baseline` bench comparison, never an in-run critic gate.

**The workspace.** A lesson is a two-column **workspace**, never a page-container/void: a frozen reading column (measure ~62ch) plus an always-full **apparatus panel** docked beside it. The whole reading+panel assembly is capped (~1500–1640px) and centered with equal gutters; "use the area" is satisfied by the full apparatus column, not by stretching prose to the viewport edge. Surfaces use the dark §0 tokens; the card↔reader morph lives on a single `#readerPanel.morph-box` wrapper that wraps the lesson iframe and is the morph's FLIP **destination** (it carries a `view-transition-name` pairing with the library card's FLIP origin). The morph is **box-only** — the container box's geometry animates while the opaque iframe contents render at the final frame (the wrapper is built in TS-20; the animation lands in TS-21).

**Four locked decisions:**
1. **Densify every section.** Each section carries a curated apparatus stack — key-term glosses (term dotted in prose, definition in the panel), a where-am-I/progress cue, a teaching mini-figure where it helps, live readouts when a component runs, sources where they exist. The working column is full on *every* section — full **because each section carries apparatus, NOT because citations exist** (typically only a fraction of sections cite a source). Apparatus must *add* what the prose doesn't already state — never filler — capped at ≤3 glosses + ≤1 mini-figure per section to avoid a dashboard.
2. **Lone element centers its text.** A genuinely standalone element with no apparatus beside it sets its text `text-align:center`, applied at the block/section level — never per-paragraph alternation inside a paired section.
3. **Cap + center the assembly.** Capped width, equal gutters; the reading + apparatus CONTENT is never stretched or pinned to the true viewport edge. **The one deliberate exception is the section scrub rail** (Figma 3:2 node 3:1120 "Navigation - Section scrubber"): it is `position: fixed` and **right-edge-pinned** (≈35px in from the viewport edge, reading as a fixed scrubber, NOT parked next to the panel), but on viewports **wider than `--frame-max` it is CLAMPED** to track the capped frame's right edge rather than orphan against a 4K viewport edge (see PR-C below — this reconciles the prior "scrubber inside the frame" intent to the Figma SoT, which wins). All other controls live inside the capped frame.
4. **Stable spine (HARD rule — wins all conflicts).** The reading column holds the *exact* same horizontal position and width for every paragraph and across section boundaries. A lone element center-aligns its text *within* the fixed column; it never moves the column. Zero left-right jitter.

**Named grid-line set (design direction — not yet pipeline-enforced).** The lesson workspace is a CSS grid whose canonical column track names are `[screen-start] [read] [gap] [panel] [scrub]`. This is the abbreviated reference set; the literal lives **here** (which wins on design conflict). The `[scrub]` track is reserved for the in-iframe dot-scrubber rail. (In the SHIPPED reader CHROME the scrub rail no longer occupies this track — it is a `position: fixed`, right-edge-pinned cluster per Figma 3:1120; see PR-C, which reconciled decision 3 to the Figma SoT. This `[scrub]` track remains the design-direction reservation for a future in-artifact workspace-rendering pass.) (A prior v11 pipeline rework had the synthesis `code` stage emit this grid and a graded critic gate on `[scrub]`; that rework was REVERTED to the blob-binary pipeline — see `docs/plans/lesson-workspace.md` — so today no pipeline stage emits or gates this literal. It remains the design target for when a future workspace-rendering pass is implemented.) The v11 reference mock materializes a fuller line set (`[screen-start] [read-start] … [read-end] [panel-start] … [panel-end] [scrub] … [screen-end]`, with an *unnamed* `var(--col-gap)` track between `[read-end]` and `[panel-start]` — there is no `[gap]` named line in the mock) as the reference implementation; the abbreviated names above are the canonical design-target set (the critic that once gated them is reverted, per above), not the only legal track labels.

**Materialized in the reader CHROME (the grid-foundation pass — PR-A).** The named-line grid above is now **rendered in the reader chrome** (`src/app/lesson/[id]/reader-shell.tsx` + `globals.css`) — the fuller mock line set (`[screen-start] edge [read-start] measure [read-end] gap [panel-start] panel [panel-end] scrub [scrub] edge [screen-end]`), capped at `--frame-max` + centered, with a per-section `grid-template-columns: subgrid` STABLE SPINE so the reading column lands on one identical `[read]` track (Δ0px, pure CSS — no JS x-math; verified at 1440/1920/390 in `e2e/lesson-workspace.spec.ts`). In this first pass the `[read]` track holds the UNCHANGED opaque-origin lesson iframe (`#readerPanel.morph-box`, `sandbox="allow-scripts"` with NO `allow-same-origin`, the strict-CSP `/artifact` route, the card→reader box-only morph destination — all byte-unchanged), and the `[panel]` (apparatus) + `[scrub]` (dot-rail) tracks render **EMPTY placeholders** — the apparatus stack and the dot-scrubber are later passes. The metrics are **component-local `--ws-*` tokens** that CONSUME the §0 geometry tokens — **no §0 retoken** (the two-copies invariant + `serve.test.ts` are not engaged). The "*not yet pipeline-enforced*" note above still holds: this is a CHROME render of the `[read]` track, not a synthesis-stage emission of the in-artifact grid (the `[panel]`/`[scrub]` content + the artifact-side grid remain the future workspace-rendering pass the named-set note describes).

**The apparatus panel (the panel-fill pass — PR-B).** The `[panel]` track now carries the **apparatus stack** and the `[scrub]` track the **section dot-rail** (`reader-shell.tsx` + `globals.css`), `container-type: inline-size` so the cards scale to `--panel-w` not the viewport. It is fed **ONLY by the SHIPPED coordinate-only `{ sections, scrollProgress }` `postMessage`** (`lesson-message.ts` is UNCHANGED — no payload extension): the **where-am-i widget**, the **section list** (moved OUT of the old flat pill strip INTO the widget), and the **scrubber labels** light up from that data, never read from the iframe DOM. **Honest degradation (the data-contract limit):** the channel carries only an *overall* `scrollProgress` scalar — there is **no posted active-section signal** — so the widget shows the two facts that scalar *truthfully* supports (the **exact overall percent** + an overall-progress fill strip, and the **posted section list**) and presents position as an explicit **estimate** ("≈ around section N of M", the scrubber dot labeled "*approx. here*"), **never** a confident tracked `NN/total` count — deriving a precise discrete section from one overall scalar would be a fabricated mapping (anti-invention). The estimate is `floor(scrollProgress × count)` (`apparatus-state.ts`, node-tested, `approximate:true`); when a future payload carries a real active-section signal the cue can be promoted to exact (`approximate` flips false) with no channel change. The **richer cards** (key-term gloss, mini-figure caption, source block, self-check readout, takeaways + re-read) render **best-effort placeholders** that clearly read as awaiting data; their real content is a coordinate-only `LessonMessage` *extension* (DELIVERED in **PR-F** — see "The apparatus DATA path" below; the chrome still never scrapes the iframe). Decision-13 best-effort: a lesson posting nothing leaves every card empty/zero and the shell fully usable. Still **no §0 retoken** (component-local `--ws-*` + the §0 catalog motion — the card reveal at `--dur-base`/`--ease`, reduced-motion gated); the trust boundary, the iframe attrs, the morph, and `lesson-message.ts` are byte-unchanged. The "*not yet pipeline-enforced*" note above still holds — this is the CHROME apparatus reading posted coordinates, not a synthesis-stage emission of in-artifact apparatus.

**The scrub rail + section jump (the scrubber pass — PR-C).** The `[scrub]` track's dot-rail is now a **vertical rail of keyboard-operable jump `<button>`s** (`reader-shell.tsx` + `globals.css`) — **one dot per SHIPPED section** (from the same coordinate-only `{ sections, scrollProgress }` `postMessage`, `lesson-message.ts` UNCHANGED), now a `position: fixed`, **RIGHT-EDGE-PINNED** rail matching **Figma 3:2 node 3:1120 "Navigation - Section scrubber"**: its right edge sits ≈35px (one `--edge-gap`) in from the VIEWPORT edge so it reads as a FIXED scrubber, it SPANS the full viewport height below the 54px topbar (`top: --ws-topbar-h`, `block-size: calc(100dvh - --ws-topbar-h)`), and its dots are a **COMPACT cluster VERTICALLY CENTERED** in that strip (`justify-content: center`) — NOT distributed across the full height (the wrong "minimap" read a prior attempt shipped and the owner rejected), NOT top-bunched, and NOT parked beside the panel. **Wide-screen CLAMP** (honoring decision 3's "no orphan at wide viewports" concern): the right inset is `calc(--ws-scrub-inset + max(0px, (100vw − --ws-frame-max)/2))`, so on viewports wider than `--frame-max` the rail tracks the capped FRAME's right edge (≈one `--edge-gap` inside it) instead of the viewport edge, never orphaning against a 4K edge; below the cap that term is 0 and the rail sits exactly at the Figma inset. The lesson scrolls INSIDE the 80vh iframe, so the rail itself NEVER moves — the **active dot** alone reflects `scrollProgress`. This SUPERSEDES the prior "inside the narrow `--scrub-w` track, never edge-pinned" placement (the `[scrub]` grid track is now vestigial; the fixed rail floats over the right gutter), reconciling §Lesson layout to the Figma SoT (which wins). The **active/done state** is encoded by **style AND `aria-label`** (the active dot scales + carries `aria-current` + an "approx. here" label; a done dot carries "(read)" — status never by color alone, §Accessibility), the active dot driven by `{ sections, scrollProgress }` (`apparatus-state.ts`, the same `floor(scrollProgress × count)` estimate). **Target size (WCAG 2.2 AA, SC 2.5.8 Target Size Minimum):** the **VISIBLE dot stays 8px** (painted by a centered `::before`), but each jump `<button>`'s OWN box — the real pointer/touch target — is enlarged to **≥ 24×24 CSS px** via transparent padding, and the row pitch is widened to **≥ 24px** (so adjacent 24px hit boxes never overlap, clearing the spacing-exception failure of the old 20px pitch). The 24px hit box is kept from widening the rail's **8px dot column** by a **negative inline margin** that collapses its horizontal layout footprint back to the 8px dot — the box overflows the column centered (never clipped), so the rail stays an 8px column at its edge-pinned inset. The active dot's `scale` rides the inner `::before`, never the button box, so the hit target is state-independent. Activating a dot posts the **coordinate-only parent→child** message `{ type: 'lesson:scrollTo', id }` INTO the iframe via `postScrollTo` (`lesson-scroll-sender.ts`). **Wire reality (the honest target-origin model — do NOT read this as "never `'*'`"):** every real jump on Chromium crosses the boundary with targetOrigin **`'*'`**. The documented intent is to target the iframe's opaque origin by its literal `'null'` token (pinned in `lesson-message.ts` `PARENT_TO_CHILD_TARGET_ORIGIN`, imported — not re-declared), but Chromium **rejects `'null'`** for an opaque-origin frame (`SyntaxError: Invalid target origin 'null'` — an opaque origin has no URL representation, so a specific-origin post to an opaque-origin frame is impossible at the platform level), so the first attempt **always throws** and the `catch` **ships on the `'*'` fallback**. The `'null'` try is kept as **forward-compat** (honored the moment any engine accepts it), **not** a runtime guarantee. The `'*'` that ships is **safe here**: the `sandbox="allow-scripts"` artifact (NO `allow-same-origin`, NO navigation) under a strict CSP cannot navigate to a foreign origin, so there is no foreign-origin frame for `'*'` to leak to. The chrome **NEVER reaches into the iframe DOM** to scroll it — the post is the only legal channel across the opaque boundary. PR-C ships the **SENDER** (best-effort): the post is harmless + inert until **PR-F** teaches the generated lesson to RECEIVE this verb and scroll itself. On the ≤900 single-column collapse the rail folds away (`globals.css` `.ws-scrub { display:none }`) and is REPLACED by the mobile TOC disclosure (PR-E, below). Still **no §0 retoken** (component-local `--ws-*` + the §0 catalog motion — the dot state swap rides `--tr-state`, reduced-motion gated); the trust boundary, the iframe attrs, the morph, and `lesson-message.ts` are byte-unchanged (the parent→child sender is a small NEW helper, not an edit to the receiver). The reading-progress bar above the grid is constrained to the SAME capped, `--edge-gap`-guttered frame the grid uses (`.reader--ws .reading-progress` — `max-inline-size:--frame-max` + `padding-inline:--edge-gap`), so the tabular `NN%` readout is never clipped at the viewport edge (the §Lesson layout anti-pattern "clipped figures or labels" + the no-horizontal-overflow-at-390 invariant). Verified in `e2e/lesson-workspace-scrub.spec.ts` (dot count == posted sections; the rail measured **right-edge-pinned ≈one edge-gap from the viewport edge @1440 and CLAMPED adjacent to the capped frame @1920** (>100px in, hugging the frame's right edge — proving it never orphans), **spanning the full height below the 54px topbar**, with the dots a **COMPACT cluster (cluster height ≪ rail height) VERTICALLY CENTERED (cluster center ≈ rail center)** — thresholds chosen to FAIL on a top-aligned or full-height-distributed layout; active-dot tracking while the fixed rail stays put; **each dot's pointer hit target ≥ 24×24 CSS px with non-overlapping adjacent targets and the 8px visible dot + the 8px column both unchanged (SC 2.5.8)**, the coordinate-only `{type:'lesson:scrollTo', id}` jump round-tripped across the opaque boundary via the seed stub's ack echo — the `'null'`-then-`'*'` target behavior is pinned at the unit level in `lesson-scroll-sender.test.ts`, since arrival alone can't distinguish the two — keyboard activation, a11y labels, 0px overflow @1440/1920/390, the ≤900 collapse).

**The integrated topbar + reading-progress hairline + Focus-reading (the topbar pass — PR-D).** The built reader route's TOP CHROME is now a single integrated **54px frosted topbar** (`reader-shell.tsx` + `globals.css`, `.ws-topbar` REUSING the shipped `.appbar` look — `--surface-header` + `backdrop-blur` + a `--surface-divider`/`--border` bottom rule) — the ONLY chrome OUTSIDE the iframe. It is a `1fr auto 1fr` grid so the wordmark stays OPTICALLY centered: **left** a `← Library` back link (a plain `<a href="/">` — the cross-document nav the card↔reader morph rides — mono, `--text-muted`→`--text` on hover); **center** the two-tone `topic·synthesis` wordmark (reuses `.appbar__wordmark` 1:1); **right** a `Jump to ⌘K` chord-HINT chip + the **Focus-reading toggle** (carrying its own `⇧F` kbd cap) + the **user pill** (reuses `.appbar__chip`/`.appbar__avatar`/`.appbar__name`, the display name derived via the SHARED `displayName` from `session-nav.tsx`). The integrated topbar SUPPRESSES the global `<SessionNav>` `.appbar` on the built reader route via a `body.has-ws-topbar` class (set by the shell on mount, cleared on unmount), so there is one bar, not two; the library + degraded routes keep the global appbar. The **reading-progress HAIRLINE** sits at `y=0` across the bar (the SHIPPED `ReadingProgress` affordance — `role="progressbar"` + `aria-valuenow` + the tabular % readout, kept for legibility-by-number but visually clipped to the hairline), a ~2.5px fill on `--brand-gradient` + `--brand-glow`, its width driven PURELY by the posted coordinate-only `scrollProgress` scalar (NO iframe DOM read), transitioning at `--tr-progress` (zeroed under reduced motion by the global guard). **Focus-reading** (the `Shift+F` chord OR the labeled button — two affordances, one state) is a pure-CHROME CSS state (`.reader--ws[data-focus]`): it hides the `[panel]` + `[scrub]` tracks and re-templates the grid to a centered, wider single reading column (`clamp(--measure, 60vw, 46rem)` — still capped + centered, nothing edge-pinned, decision 3); it is chrome-local toggle state — **NO `postMessage`**, the morph + `lesson-message.ts` UNCHANGED. **Responsive ≤640** (DESIGN.md invariant "the wordmark is hidden ≤640"): the wordmark + the `Jump to ⌘K` chip hide and the Focus-reading control compacts to its `⇧F` glyph (its accessible name carried by `aria-label`), so the bar never overflows; the user pill alone anchors the right. **a11y:** `role="progressbar"` + `aria-valuenow`; the Focus-reading control is labeled + keyboard-operable (`Shift+F` + the button, `aria-pressed` + `aria-keyshortcuts`); focus-visible rings on the back link + the toggle; the pressed state reads by STYLE + label, not color alone. Still **no §0 retoken** (component-local `--ws-topbar-h`/`--ws-progress-h` + reuse of the §0/appbar tokens — the two-copies invariant + `serve.test.ts` are not engaged); the trust boundary, the iframe attrs, the morph, and `lesson-message.ts` are byte-unchanged. Verified in `e2e/lesson-workspace-topbar.spec.ts` (the topbar present + ~54px + the 3-track grid; the posted `scrollProgress` moving the hairline fill width + setting `aria-valuenow`; Shift+F + the button hiding `[panel]`+`[scrub]` and widening the spine, restoring on toggle; the ≤640 wordmark+chips fold; focus-visible rings; reduced-motion zeroing the hairline transition; 0px overflow @1440/1920/390).

**Responsive collapse + mobile TOC + a11y/reduced-motion hardening (the final chrome pass — PR-E).** The two-column workspace **collapses to a single column ≤900px** (`globals.css` `@media (max-width:60rem)`): the `[read]`/`[panel]`/`[scrub]` tracks become block flow and the **apparatus panel reflows DIRECTLY UNDER the prose it annotates** (`.ws-panel { margin-top }`), with **0px horizontal overflow at 390/768/900**. Because the in-frame `[scrub]` dot-rail has no room in one column, it folds away (`.ws-scrub { display:none }`) and is REPLACED by a **mobile section-jump TOC** (`reader-shell.tsx` `.ws-toc` + `globals.css`, shown ONLY ≤900) — a labeled **disclosure**: a "Sections" toggle (`aria-expanded` + `aria-controls`, a rotating chevron) over a collapsible list of jump controls that carries the `hidden` attribute while closed (so it leaves BOTH the a11y tree and the tab order). Tapping a TOC item posts the **SAME coordinate-only parent→child** `{ type:'lesson:scrollTo', id }` the desktop dots use (`postScrollTo` → target `'null'` → `'*'` fallback) and then collapses the disclosure, so the section jump survives the collapse; the chrome NEVER reaches into the iframe DOM. Every TOC control meets the **WCAG 2.2 SC 2.5.8 ≥24×24** target floor. The a11y pass is holistic: a **logical `topbar→prose→panel→scrub` tab order** (DOM order, NO positive `tabindex`), SR landmarks/labels on every region (the topbar, the apparatus `aside`, both section `nav`s), and state by aria (`aria-current`/`aria-pressed`/`aria-expanded`/`aria-valuenow`), never color alone — proven by an **AXE scan with zero serious/critical violations at desktop + mobile**. **Reduced-motion completeness:** every workspace transition (the hairline, the focus-widen, the card reveal, the scrub/dot state, the TOC chevron) is zeroed under `prefers-reduced-motion:reduce` and the card→reader morph's View-Transition transport is declared ONLY under no-preference. Focus-reading also hides the TOC. Still **no §0 retoken** (component-local `--ws-toc-*` + the §0 catalog motion); the trust boundary, the iframe attrs, the morph, and `lesson-message.ts` are byte-unchanged — the only `lesson-message.ts` touch is a **COMMENT-ONLY** reconcile distinguishing the iframe→parent progress sender (known origin, never `'*'`) from the parent→child scrollTo sender (the sound `'null'`→`'*'` fallback for the opaque, non-navigable sandbox). Verified in `e2e/lesson-workspace-responsive.spec.ts` (the ≤900 collapse with the apparatus measured below the prose + 0px overflow @390/768/900; the TOC discloses + a tap posts the coordinate-only `lesson:scrollTo` across the boundary + ≥24×24 controls; the logical tab order; reduced motion zeroing the transitions; the AXE scan at desktop + mobile).

**The apparatus DATA path — richer cards light up with real lesson content (PR-F).** The richer apparatus cards (**key terms** · **figure caption** · **source** · **self-check** · **takeaways**) — best-effort placeholders since PR-B — now render **REAL lesson data** from an **OPTIONAL coordinate-only `apparatus` EXTENSION** to the `{ sections, scrollProgress }` `postMessage` (`lesson-message.ts` — `LessonMessage.apparatus?`). The in-iframe **SENDER** (the `CODE_SYSTEM` synthesis prompt, `src/pipeline/code.ts` — the ONLY pipeline change) serializes the values the lesson already contains (its glosses `{term,definition}`, figure `{caption}`, sources `{title,url}`, self-checks `{prompt,answer}`, and takeaways `[string]`) as **bounded TEXT-only data** in the SAME `lesson:progress` message — never DOM node refs, never HTML. The **RECEIVE side** (`validateMessage`) sanitizes the apparatus as untrusted data: bounded per-field counts + string lengths, `http(s)`-only source URLs, each entry rebuilt with only its contract fields (no extra property rides in). The panel renders every value as React-escaped **TEXT**; a source `url` becomes a validated **`rel="noopener noreferrer"` link** (never `dangerouslySetInnerHTML`), and the self-check answer is gated behind the prompt as a keyboard-operable native `<details>`. **Fail-safe + backward-compatible (decision-13):** a missing / partial / malformed / oversized apparatus is sanitized down to the valid subset (or dropped) and the affected cards fall back to their **placeholders** — NEVER a whole-message rejection (the reading-progress/section data still flows), never a crash, never a fabricated value; a lesson posting only the old `{ sections, scrollProgress }` shape renders exactly as before. The chrome **still NEVER reads the iframe DOM** (coordinate-only); the **trust boundary** (the `sandbox="allow-scripts"` opaque-origin frame, the strict `ARTIFACT_CSP`, owner-scoping), the **iframe attrs**, and the **card→reader morph** are byte-unchanged, and there is **no §0 retoken** (component-local card styles over the existing §0 tokens). Verified in `e2e/lesson-workspace-apparatus.spec.ts` (the richer cards render the real glosses/figure/source-link/self-check/takeaways; the self-check gate reveals on activation; the old shape falls back to placeholders), the `lesson-message.test.ts` security matrix (malformed/oversized/`javascript:`-URL apparatus is sanitized away; identity still gates a foreign window), and the `code.test.ts` `CODE_SYSTEM` prompt-contract test (the sender instructs the exact apparatus emission).

**Per-section composition** (decided per section, not one global pick):
- **With apparatus** (≈all sections post-densify): a 2-column unit — frozen prose spine on the left (text left-aligned), apparatus docked in the right panel track beside it.
- **Without apparatus** (rare): prose stays on the same fixed spine, text center-aligned, panel track absent — the spine does not move.

**Invariants:** two columns max — never one, never three. Interactive components (the Play interactive) get a full-width bounded plate, fully visible, with no prose overlap. Citations render beside their text in the panel, never a lone card in a void. Responsive: collapse to one column ≤~900px (apparatus reflows directly under the prose it annotates); no horizontal overflow at 390 (the `.appbar` fits within the viewport; the wordmark is hidden ≤640). Reduced-motion + a11y are correctness, not polish (§Motion, §Accessibility).

**Anti-patterns (rejected — must not recur):** reserved/empty margin · single column · prose-over-component occlusion · per-paragraph horizontal jitter · lopsided/left-pinned prose with dead right · an edge-pinned lone element at wide viewports (the section scrub rail is edge-pinned BY DESIGN per Figma 3:1120, but is CLAMPED past `--frame-max` to track the capped frame's right edge so it never orphans at a wide/4K edge — see decision 3 + PR-C) · clipped figures or labels.

**Provenance.** The scratch constraint ledger `.superpowers/lesson-layout-ledger.md` (LOCKED 2026-06-24) is **promoted** into this section — `DESIGN.md` `## Lesson layout` is now the source of truth and wins on any conflict; the scratch ledger is retained as the provenance record only, not the SoT.

## Accessibility
Target WCAG 2.2 AA. Visible `:focus-visible` ring (2px `--interactive`). Full keyboard operability of the form + hub. Status by label+icon, not color alone. Reduced motion honored (§Motion). Generated artifacts carry their **own** a11y contract (a generation target — see `docs/plans/`); the chrome never depends on an iframe's internals for its own accessibility.
